name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  repository-foundation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify repository foundation
        run: |
          test -f README.md
          test -f LICENSE
          test -f CONTRIBUTING.md
